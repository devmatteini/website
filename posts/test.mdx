---
title: MDX Test
date: 2023-01-01
status: draft
description: "This is a very long description to test that ellipse works and doesn't break anything on the post card"
---

Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
Faucibus purus in massa tempor nec feugiat nisl pretium.
Arcu non sodales neque sodales ut etiam. Quisque sagittis purus sit amet.
Enim ut sem viverra aliquet eget sit amet tellus cras.

# Heading (rank 1)

## Heading 2

### 3

#### 4

##### 5

###### 6

> Block quote

-   Unordered
-   List

1. Ordered
2. List

A paragraph, introducing a thematic break:

---

Code block:

```rust showLineNumbers
pub fn get_release(
    client: &GithubClient,
    repository: &Repository,
    tag: Option<&Tag>,
) -> Result<Release, GithubError> {
    let url = get_release_url(repository, tag);
    client
        .get(&url)
        .timeout(Duration::from_secs(5))
        .call()
        .map_err(GithubError::from)
        .and_then(deserialize)
        .map(to_release(repository))
}
```

Code block with title:

```js title="foo.js"
const foo = () => "foo"
```

Code block with highlighted lines and line numbers:

```js {1,4} showLineNumbers
const foo = () =>
    F.pipe(
        [1, 2, 3, 4, 5, 6],
        A.filter(isEven),
        A.map(multiplyBy(2)),
    )
```

Code block with highlighted words:

```js /foo/ /filter/
const foo = () =>
    F.pipe(
        [1, 2, 3, 4, 5, 6],
        A.filter(isEven),
        A.map(multiplyBy(2)),
    )
```

a [link](https://example.com), some _emphasis_,
something **strong**, and finally a little `code()`.

Some internal link [homepage](/) or hash link [heading 2](#heading-2)
