---
import { cn } from "../shared/cn"

interface Props {
    className?: string
}

const { className } = Astro.props
---

<div class={cn("max-w-4xl px-4 mx-auto flex flex-col", className)}>{(<slot />)}</div>
